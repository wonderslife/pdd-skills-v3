# {{ module_name }} - API层测试用例

> **模块编码**: {{ module_code }}
> **模块名称**: {{ module_name }}
> **测试层级**: Level 1 - API单接口测试
> **生成时间**: {{ generated_at }}
> **API端点数**: {{ api_endpoints | length }}
> **用例总数**: {{ test_cases | length }}

---

## 一、测试范围

### 1.1 API端点清单

| 编号 | 方法 | 路径 | 权限标识 | 描述 |
|------|------|------|---------|------|
{% for ep in api_endpoints %}| {{ ep.id }} | {{ ep.method }} | {{ ep.path }} | {{ ep.permission }} | {{ ep.description }} |
{% endfor %}

### 1.2 状态码定义

| 状态码 | 含义 |
|--------|------|
{% for code in status_codes %}| {{ code.code }} | {{ code.meaning }} |
{% endfor %}

### 1.3 测试用户角色

| 角色 | 用户名 | 部门 | 用途 |
|------|--------|------|------|
{% for user in test_users %}| {{ user.role }} | {{ user.username }} | {{ user.dept }} | {{ user.purpose }} |
{% endfor %}

---

## 二、测试用例

{% for tc in test_cases %}
### {{ tc.id }} {{ tc.title }}

| 属性 | 值 |
|------|-----|
| **用例ID** | {{ tc.id }} |
| **测试目的** | {{ tc.purpose }} |
| **优先级** | {{ tc.priority }} |
| **类型** | {{ tc.type }} |
| **前置条件** | {{ tc.preconditions }} |
| **操作步骤** | {{ tc.steps }} |
| **预期结果** | {{ tc.expected }} |
| **实际结果** | （留空，执行时填写） |
| **状态** | 待执行 |

{% endfor %}

---

## 三、权限矩阵

| API编号 | 操作 | {{ test_users[0].role }} | {{ test_users[1].role }} | {{ test_users[2].role }} | {{ test_users[3].role }} | {{ test_users[4].role }} |
|---------|------|---------|---------|---------|---------|---------|
{% for ep in api_endpoints %}| {{ ep.id }} | {{ ep.description }} | ✅/❌ | ✅/❌ | ✅/❌ | ✅/❌ | ✅/❌ |
{% endfor %}

---

## 四、源码关键发现

| 编号 | 发现项 | 源码位置 | 影响范围 | 测试建议 |
|------|--------|---------|---------|---------|
{% for finding in source_findings %}| {{ finding.id }} | {{ finding.item }} | {{ finding.location }} | {{ finding.impact }} | {{ finding.suggestion }} |
{% endfor %}
