# {{ module_name }} - 边界异常测试用例

> **模块编码**: {{ module_code }}
> **模块名称**: {{ module_name }}
> **测试层级**: Level 5 - 边界异常测试
> **生成时间**: {{ generated_at }}
> **用例总数**: {{ test_cases | length }}

---

## 一、测试范围

### 1.1 边界异常分类

| 分类 | 描述 | 用例数 |
|------|------|--------|
| 空值/Null | 必填字段传入null或空字符串 | {{ null_case_count }} |
| 越界值 | 数值超出允许范围 | {{ overflow_case_count }} |
| 格式错误 | 字段格式不符合规范 | {{ format_case_count }} |
| 权限越权 | 无权限/跨角色/跨部门访问 | {{ permission_case_count }} |
| 并发冲突 | 同一资源并发操作 | {{ concurrency_case_count }} |
| 数据依赖 | 前置数据不存在或已变更 | {{ dependency_case_count }} |

### 1.2 Bug模式对照

| Bug模式ID | 模式名称 | 对应用例 |
|-----------|---------|---------|
{% for bug in bug_patterns %}| {{ bug.id }} | {{ bug.name }} | {{ bug.test_case_ids }} |
{% endfor %}

---

## 二、测试用例

{% for tc in test_cases %}
### {{ tc.id }} {{ tc.title }}

| 属性 | 值 |
|------|-----|
| **用例ID** | {{ tc.id }} |
| **测试目的** | {{ tc.purpose }} |
| **优先级** | {{ tc.priority }} |
| **类型** | {{ tc.type }} |
| **异常类别** | {{ tc.exception_category }} |
| **前置条件** | {{ tc.preconditions }} |
| **异常输入** | {{ tc.abnormal_input }} |
| **操作步骤** | {{ tc.steps }} |
| **预期结果** | {{ tc.expected }} |
| **实际结果** | （留空，执行时填写） |
| **状态** | 待执行 |

{% endfor %}
