# {{ module_name }} - 集成层测试用例

> **模块编码**: {{ module_code }}
> **模块名称**: {{ module_name }}
> **测试层级**: Level 2 - 集成测试
> **生成时间**: {{ generated_at }}
> **用例总数**: {{ test_cases | length }}

---

## 一、测试范围

### 1.1 集成测试维度

| 维度 | 描述 | 用例数 |
|------|------|--------|
| 工作流集成 | 审批流程节点流转与状态变更 | {{ workflow_case_count }} |
| 数据一致性 | 跨表/跨服务数据一致性验证 | {{ consistency_case_count }} |
| 权限集成 | 角色权限在完整流程中的验证 | {{ permission_case_count }} |

### 1.2 工作流配置

| 节点ID | 节点名称 | 处理人类型 | 处理人值 | 必要文件 |
|--------|---------|-----------|---------|---------|
{% for node in workflow_nodes %}| {{ node.id }} | {{ node.name }} | {{ node.assignee_type }} | {{ node.assignee_value }} | {{ node.required_files | default('无') }} |
{% endfor %}

---

## 二、测试用例

{% for tc in test_cases %}
### {{ tc.id }} {{ tc.title }}

| 属性 | 值 |
|------|-----|
| **用例ID** | {{ tc.id }} |
| **测试目的** | {{ tc.purpose }} |
| **优先级** | {{ tc.priority }} |
| **类型** | {{ tc.type }} |
| **前置条件** | {{ tc.preconditions }} |
| **操作步骤** | {{ tc.steps }} |
| **预期结果** | {{ tc.expected }} |
| **实际结果** | （留空，执行时填写） |
| **状态** | 待执行 |

{% endfor %}

---

## 三、状态转移覆盖矩阵

| 起始状态 | 目标状态 | 触发操作 | 覆盖用例 |
|---------|---------|---------|---------|
{% for trans in state_transitions %}| {{ trans.from }} | {{ trans.to }} | {{ trans.trigger }} | {{ trans.test_case_id }} |
{% endfor %}
