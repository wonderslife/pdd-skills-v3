# {{ module_name }} - 测试用例集总览

> **模块编码**: {{ module_code }}
> **模块名称**: {{ module_name }}
> **生成时间**: {{ generated_at }}
> **框架**: {{ framework }}

---

## 一、用例统计

### 1.1 各层级用例分布

| 层级 | 文档 | 用例数 | 可运行代码 | pytest函数数 |
|------|------|--------|-----------|-------------|
| Level 1 - API单接口 | [01-api-testcases.md](docs/01-api-testcases.md) | {{ api_case_count }} | 01-api-testcases/ | {{ api_pytest_count }} |
| Level 2 - 集成测试 | [02-integration-testcases.md](docs/02-integration-testcases.md) | {{ integration_case_count }} | 02-integration-testcases/ | {{ integration_pytest_count }} |
| Level 3 - UI界面 | [03-ui-testcases.md](docs/03-ui-testcases.md) | {{ ui_case_count }} | 03-ui-testcases/ | {{ ui_pytest_count }} |
| Level 4 - E2E场景 | [04-e2e-testcases.md](docs/04-e2e-testcases.md) | {{ e2e_case_count }} | 04-e2e-testcases/ | {{ e2e_pytest_count }} |
| Level 5 - 边界异常 | [05-boundary-exception-testcases.md](docs/05-boundary-exception-testcases.md) | {{ boundary_case_count }} | - | - |
| Level 6 - 性能测试 | [06-performance-testcases.md](docs/06-performance-testcases.md) | {{ performance_case_count }} | - | - |
| **合计** | - | **{{ total_case_count }}** | - | **{{ total_pytest_count }}** |

### 1.2 优先级分布

| 优先级 | 用例数 | 占比 |
|--------|--------|------|
| P0 (阻塞) | {{ p0_count }} | {{ p0_ratio }}% |
| P1 (重要) | {{ p1_count }} | {{ p1_ratio }}% |
| P2 (一般) | {{ p2_count }} | {{ p2_ratio }}% |

---

## 二、快速开始

### 2.1 环境准备

```bash
cd 01-api-testcases
pip install -r requirements.txt
```

### 2.2 配置修改

编辑 `config.py`，修改以下配置：

```python
BASE_URL = "http://your-server:port"
```

### 2.3 运行测试

```bash
# 运行全部API测试
pytest -v

# 运行指定优先级
pytest -v -m p0

# 运行指定类型
pytest -v -m functional

# 生成Allure报告
pytest --alluredir=./allure-results
allure serve ./allure-results
```

---

## 三、测试数据

测试数据定义在 [test-data.yaml](test-data.yaml) 中，包含：

| 数据类型 | 数量 | 说明 |
|---------|------|------|
| 测试用户 | {{ test_user_count }} | 覆盖5个基础角色 |
| 表单数据变体 | {{ form_variant_count }} | 有效/无效/边界值 |
| 审批操作数据 | {{ approval_data_count }} | 同意/驳回/无意见 |
| 文件数据 | {{ file_data_count }} | 合法/非法文件类型 |
| 工作流配置 | {{ workflow_config_count }} | 节点+处理人映射 |

---

## 四、Markers说明

| Marker | 用途 | 示例 |
|--------|------|------|
| `@pytest.mark.p0` | P0阻塞级用例 | 核心CRUD操作 |
| `@pytest.mark.p1` | P1重要级用例 | 权限验证 |
| `@pytest.mark.p2` | P2一般级用例 | 边界值测试 |
| `@pytest.mark.functional` | 功能测试 | 正常业务流程 |
| `@pytest.mark.permission` | 权限测试 | 越权/无权限 |
| `@pytest.mark.exception` | 异常测试 | 参数错误/服务异常 |
| `@pytest.mark.boundary` | 边界测试 | 极值/null/空 |
| `@pytest.mark.integration` | 集成测试 | 跨模块/工作流 |
| `@pytest.mark.workflow` | 工作流测试 | 审批流转 |
| `@pytest.mark.concurrency` | 并发测试 | 竞态条件 |
| `@pytest.mark.regression` | 回归测试 | 历史缺陷验证 |

---

## 五、覆盖率预估

| 维度 | 目标 | 预估值 |
|------|------|--------|
| API端点覆盖 | 100% | {{ api_coverage }}% |
| 权限场景覆盖 | ≥95% | {{ permission_coverage }}% |
| 状态转移覆盖 | ≥90% | {{ state_coverage }}% |
| 代码行覆盖 | ≥80% | 待运行 |
| 分支覆盖 | ≥70% | 待运行 |
