# {{ module_name }} - E2E场景测试用例

> **模块编码**: {{ module_code }}
> **模块名称**: {{ module_name }}
> **测试层级**: Level 4 - 端到端场景测试
> **生成时间**: {{ generated_at }}
> **用例总数**: {{ test_cases | length }}

---

## 一、测试范围

### 1.1 E2E场景定义

E2E场景测试验证从用户操作入口到最终数据落库的完整业务链路，确保各子系统协同工作正常。

### 1.2 场景覆盖策略

| 策略 | 描述 | 场景数 |
|------|------|--------|
| Happy Path | 正常业务全流程 | {{ happy_path_count }} |
| Rejection Path | 驳回重提流程 | {{ rejection_path_count }} |
| Termination Path | 终止流程 | {{ termination_path_count }} |
| Cross-Role Path | 跨角色协作流程 | {{ cross_role_count }} |

---

## 二、测试用例

{% for tc in test_cases %}
### {{ tc.id }} {{ tc.title }}

| 属性 | 值 |
|------|-----|
| **用例ID** | {{ tc.id }} |
| **测试目的** | {{ tc.purpose }} |
| **优先级** | {{ tc.priority }} |
| **类型** | E2E场景测试 |
| **涉及角色** | {{ tc.roles }} |
| **涉及API** | {{ tc.api_sequence }} |
| **前置条件** | {{ tc.preconditions }} |
| **操作步骤** | {{ tc.steps }} |
| **预期结果** | {{ tc.expected }} |
| **实际结果** | （留空，执行时填写） |
| **状态** | 待执行 |

{% endfor %}

---

## 三、场景与API映射

| E2E场景 | API调用链 | 状态流转路径 |
|---------|----------|-------------|
{% for scenario in e2e_scenarios %}| {{ scenario.name }} | {{ scenario.api_chain }} | {{ scenario.state_path }} |
{% endfor %}
