# {{ module_name }} 测试用户配置
# Auto-generated by expert-testcases v{{ skill_version }}

users:
  - role: "applicant"
    username: "{{ applicant_username }}"
    password: "{{ applicant_password }}"
    dept: "{{ applicant_dept }}"
    dept_id: {{ applicant_dept_id }}
    fixture_name: "applicant_session"
    description: "申请人 - 发起业务申请"

  - role: "dept_manager"
    username: "{{ dept_manager_username }}"
    password: "{{ dept_manager_password }}"
    dept: "{{ dept_manager_dept }}"
    dept_id: {{ dept_manager_dept_id }}
    fixture_name: "dept_manager_session"
    description: "子公司审批人 - 审批/驳回申请"

  - role: "leader"
    username: "{{ leader_username }}"
    password: "{{ leader_password }}"
    dept: "{{ leader_dept }}"
    dept_id: {{ leader_dept_id }}
    fixture_name: "leader_session"
    description: "集团审批人 - 最终审批决策"

  - role: "admin"
    username: "{{ admin_username }}"
    password: "{{ admin_password }}"
    dept: "{{ admin_dept }}"
    dept_id: {{ admin_dept_id }}
    fixture_name: "admin_session"
    description: "管理员 - 全局管理权限"

  - role: "cross_dept_user"
    username: "{{ cross_dept_username }}"
    password: "{{ cross_dept_password }}"
    dept: "{{ cross_dept_dept }}"
    dept_id: {{ cross_dept_dept_id }}
    fixture_name: "cross_dept_session"
    description: "跨部门用户 - 数据权限隔离验证"
