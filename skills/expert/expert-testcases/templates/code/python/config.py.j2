"""
{{ module_name }} - {{ level_name }} test configuration

Auto-generated by expert-testcases v{{ skill_version }}
"""
import os

BASE_URL = os.getenv("TEST_BASE_URL", "{{ base_url }}")
API_PREFIX = "{{ api_prefix }}"
TIMEOUT = int(os.getenv("TEST_TIMEOUT", "30"))
VERIFY_SSL = os.getenv("TEST_VERIFY_SSL", "false").lower() == "true"

{% if has_workflow %}
WORKFLOW_PROCESS_KEY = "{{ workflow_process_key }}"
{% endif %}

MODULE_CODE = "{{ module_code }}"
MODULE_NAME = "{{ module_name }}"

STATUS_CODES = {
{% for code in status_codes %}
    "{{ code.code }}": "{{ code.meaning }}",
{% endfor %}
}
